# use of noImplicitAny

it forces to explicitly define types for every variable with any type

# use of strictNullChecks

it prevents null and defined from being accidentally assigned to a type

# use of strictFunctionTypes

it ensure functions are safely assigned

# use of noUncheckedIndexedAccess

it appends a undefined type to a variable which helps in handling error in future
